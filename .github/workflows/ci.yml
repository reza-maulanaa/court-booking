name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Batalkan run lama kalau ada push baru ke branch/PR yang sama.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Hanya unit test (src/**) — test/booking-api.test.ts butuh server
      # hidup + 2 akun & 1 lapangan yang di-seed manual, belum ada script
      # seed otomatis di repo ini. Jalankan integrasi itu manual di lokal.
      - run: npx vitest run src --passWithNoTests

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # `next build` sudah menjalankan typecheck penuh sendiri (next.config.ts
      # tidak set typescript.ignoreBuildErrors) — job "typecheck" terpisah
      # sengaja tidak ada: RouteContext & tipe route lain digenerate Next
      # sebagai bagian dari build, jadi `tsc --noEmit` berdiri sendiri di
      # checkout bersih (tanpa .next/types) akan gagal palsu. Script
      # `npm run typecheck` tetap ada di package.json untuk iterasi lokal
      # cepat (biasanya sudah ada .next/types dari `next dev` sebelumnya).
      - run: npm run build
        env:
          # Nilai bohongan cukup — semua route DB (termasuk "/") pakai
          # `export const dynamic = "force-dynamic"` / route dinamis,
          # jadi tidak ada query yang benar-benar jalan saat build,
          # cuma perlu string non-kosong biar neon() tidak throw.
          DATABASE_URL: postgresql://ci:ci@localhost:5432/ci
          JWT_SECRET: ci-build-placeholder-not-a-real-secret
